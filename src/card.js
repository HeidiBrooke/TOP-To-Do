    const card = (name, deck) => {
        const cardName = name;
        const cardSteps = [];
        const cardDate = '';
        const cardCompleteValue = 0;
        const cardDeck = deck;
        return {
            cardName, cardCompleteValue, cardDate, cardSteps, cardDeck
        };
    }



export default card;