const card = (name) => {
    const cardName = name;
    const cardSteps = [];
    const cardDate = '';
    const cardCompleteValue = 0;
    const cardDeck = ["all"];
    return {
            cardName, cardCompleteValue, cardDate, cardSteps, cardDeck
        }
    };
export default card;
